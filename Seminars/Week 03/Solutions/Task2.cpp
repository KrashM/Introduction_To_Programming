#include <iostream>

int main(){

    int n, k, mask = 1;
    std::cin >> n >> k;
    
    if(k > n){

        std::cout << "false\n";
        return 0;

    }

    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;
    if(mask >= k){

        std::cout << "true\n";
        return 0;

    }
    if(n & mask ^ k & mask){

        std::cout << "false\n";
        return 0;

    }
    mask <<= 1;

    return 0;

}